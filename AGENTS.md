### Go 语言 AI 开发规范（通用版）

本文件是 Go 项目的 AI 协作与工程开发规范。任何代码生成、修改、评审、测试与文档更新都应优先遵循本文件；若本规范与当前仓库的项目级规则、用户临时需求或业务约束冲突，应先说明冲突并请求确认。

---

#### 1. 项目理解与边界

- **先理解再修改**：修改前应阅读相关入口、调用链、配置、测试和文档，不凭文件名猜实现。
- **识别项目类型**：先确认项目是 CLI、HTTP 服务、RPC 服务、库、批处理任务、桌面工具还是混合形态，再决定实现方式。
- **识别运行环境**：确认目标平台、Go 版本、构建方式、部署方式、是否启用 CGO、是否依赖系统库或外部服务。
- **识别模块信息**：以 `go.mod` 为准确认 module path、Go 版本和依赖；不要假设包路径、模块名或工具链版本。
- **尊重既有架构**：优先沿用仓库已有目录布局、框架、依赖注入方式、日志库、配置格式和测试风格。
- **边界清晰**：
  - `cmd/` 通常放程序入口、参数解析和启动编排，不承载复杂业务逻辑。
  - `internal/` 放仅供本仓库使用的实现细节，保持包职责清晰，避免循环依赖。
  - `pkg/` 仅放确实需要被外部复用的稳定能力，不把普通内部代码提前暴露。
  - `configs/`、`docs/`、`scripts/`、`testdata/` 等目录应遵循仓库已有约定。
- **保护用户改动**：不得回滚或覆盖用户未要求修改的内容；遇到无关改动应忽略，遇到相关改动应基于现状继续。
- **仓库产物约束**：不要提交生成的二进制、日志、临时报告、缓存、覆盖率文件、构建产物和本地配置，除非用户明确要求。

---

#### 2. 核心 Go 风格

- **官方规范优先**：遵循 Effective Go、Go Code Review Comments 和标准库常见写法。
- **格式化**：所有 Go 代码必须通过 `gofmt` 或 `goimports` 格式化；禁止手动对齐空格。
- **命名**：
  - 使用 CamelCase，不使用下划线。
  - 导出名称首字母大写，非导出名称首字母小写。
  - 常见缩写保持一致：`ID`、`HTTP`、`URL`、`URI`、`SQL`、`DB`、`JSON`、`XML`、`API`、`RPC`、`TLS`。
  - 单方法接口通常以 `-er` 结尾，如 `Reader`、`Writer`；避免 `IReader`、`ReaderInterface`。
- **注释**：
  - 导出的包、类型、函数、方法、常量、变量应有文档注释，并以被注释名称开头。
  - 注释使用完整英文句子并以句号结尾；项目说明、业务文档和需求说明可以使用中文。
  - 不写显而易见的注释；复杂约束、兼容性原因、安全原因使用 `// NOTE:` 简要说明。
- **代码组织**：
  - 包名短小、单数、全小写，不使用 `util`、`common` 这类含糊包名，除非已有明确职责。
  - 文件拆分服务于职责和可读性，不按“一个函数一个文件”机械拆分。
  - 避免过早抽象；只有当抽象能减少真实复杂度、隔离变化或匹配既有模式时才引入。

---

#### 3. 错误处理与日志

- **错误必须显式处理**：不得忽略 `error`。如果确实要忽略，使用 `_ =` 并写明原因。
- **错误包装**：跨层返回错误时使用 `fmt.Errorf("context: %w", err)`，保留错误链。
- **错误判断**：需要区分错误类型时优先使用 `errors.Is`、`errors.As` 或明确的 sentinel error。
- **错误消息**：
  - 错误文本小写开头，不以标点结尾，除非包含专有名词。
  - 错误上下文应说明操作对象，如 `open config`、`decode request`、`query user`、`write response`。
- **Panic 限制**：业务逻辑禁止用 `panic/recover` 替代错误处理。仅在启动期不可恢复的编程错误或配置错误中谨慎使用，并优先返回错误。
- **日志规范**：
  - 沿用项目既有日志库和字段风格；如果项目已使用结构化日志，应继续使用结构化字段。
  - 不在日志中输出密码、token、密钥、Cookie、完整连接串、个人隐私、完整请求体或完整 SQL 参数等敏感信息。
  - HTTP/RPC handler 返回给客户端的错误信息应简洁、安全；详细错误写入服务端日志。
  - 日志级别要稳定：可恢复业务失败通常为 warn，系统不可用或关键流程失败为 error，正常状态变化为 info，调试细节为 debug。

---

#### 4. Context、并发与资源生命周期

- **Context 传递**：可能阻塞、访问 I/O、数据库、网络或等待锁的函数，应将 `context.Context` 作为第一个参数，命名为 `ctx`。
- **Context 使用**：
  - 不把 `context.Context` 存入结构体，除非项目已有明确生命周期设计。
  - 不使用 `context.Background()` 偷换调用方传入的取消、超时和 trace 信息。
  - 超时和取消应靠调用链传递，避免在深层函数随意创建不可配置的长超时。
- **Goroutine 生命周期**：
  - 不启动无退出机制的 goroutine。
  - 使用 `context.Context`、`sync.WaitGroup`、`errgroup` 或服务生命周期统一管理退出。
  - goroutine 内部错误要可观测、可返回或可上报，不要静默丢失。
- **Channel 约定**：
  - 明确 channel 的创建者、写入者和关闭者；只由发送方关闭 channel。
  - 不用 nil channel、未缓冲 channel 或 select default 制造难以维护的隐式控制流，除非有清晰注释。
- **资源关闭**：
  - `rows.Close()`、`db.Close()`、文件句柄、HTTP body、消息订阅、定时器等必须关闭并处理关键错误。
  - `defer` 可用于普通清理；避免在高频循环中使用 `defer`。
  - 读取数据库 rows 后必须检查 `rows.Err()`。
- **优雅退出**：服务级变更不得破坏现有 SIGINT/SIGTERM、HTTP shutdown、worker 停止、队列 drain、日志 flush 等流程。

---

#### 5. 输入校验、数据访问与安全

- **外部输入不可信**：URL path、query string、headers、body、配置项、环境变量、文件内容、消息队列数据和数据库返回值都视为不可信输入。
- **HTTP/RPC 输入**：
  - 请求参数必须校验类型、范围、长度、枚举值、必填项和默认值。
  - 请求体大小应有上限；上传和流式处理应考虑超时、取消和临时文件清理。
  - 响应错误应避免泄露内部路径、SQL、堆栈、密钥和依赖服务细节。
- **SQL 与数据访问**：
  - 禁止拼接外部输入为 SQL 值；值必须使用参数绑定。
  - 动态表名、列名、排序字段等无法参数化的标识符必须通过白名单、枚举或元数据校验。
  - 分页、排序、时间范围和批量查询必须设置合理边界，避免一次性读取过多数据。
  - 时间参数应解析为 `time.Time` 后绑定；时区语义必须明确使用本机时区、UTC 或配置指定时区。
- **文件与路径**：
  - 处理用户提供的路径时防止路径遍历，必要时使用固定根目录和 `filepath.Clean` 后校验。
  - 不把用户输入直接用于执行命令、打开任意文件或拼接 shell 命令。
- **敏感信息**：
  - 配置、日志、错误、监控和测试数据不得泄露真实密钥。
  - 新增认证、授权、签名、加密相关逻辑时优先使用标准库或成熟依赖，不手写密码学实现。

---

#### 6. 接口、结构体与可维护性

- **保持简单**：没有明确需求时，不引入额外抽象层、框架、代码生成或复杂设计模式。
- **依赖注入**：通过构造函数 `NewXxx` 注入依赖；避免复杂 `init()` 和可变全局状态。
- **接口设计**：
  - 优先在调用侧定义小接口；接受接口，返回具体类型。
  - 接口应描述行为，不描述实现来源。
  - 公共接口一旦暴露要保持稳定，修改时同步文档、示例和测试。
- **结构体设计**：
  - 新结构体尽量让零值安全；若零值不可用，构造函数和注释必须说明。
  - 配置结构体应有明确默认值、校验和文档。
  - JSON/TOML/YAML tag 应稳定，避免无意破坏外部兼容。
- **方法接收者**：
  - 小且不可变的结构体可用值接收者。
  - 需要修改状态、包含锁、连接、缓存或结构体较大时使用指针接收者。
  - 同一类型的方法接收者尽量保持一致。
- **兼容性**：修改 HTTP API、RPC schema、JSON 字段、配置文件、环境变量、CLI flag、数据库 schema 时，必须同步 README、示例、迁移说明和测试。

---

#### 7. 性能与内存

- **标准库优先**：引入第三方依赖前，先确认标准库是否足够。
- **预分配**：已知容量时使用 `make([]T, 0, n)` 或 `make(map[K]V, n)`。
- **字符串构造**：循环或多段拼接使用 `strings.Builder`；少量简单拼接可以使用 `+`。
- **热路径谨慎分配**：数据库行扫描、HTTP 请求处理、日志字段构造、序列化和循环任务中避免不必要的临时对象。
- **I/O 与内存边界**：
  - 避免无上限 `io.ReadAll` 读取外部输入；必要时使用 `io.LimitReader` 或配置限制。
  - 大文件、大响应、批量数据优先流式处理。
- **并发性能**：
  - 锁保护范围尽量小，但不要牺牲正确性。
  - 使用 `sync.Map` 前确认访问模式适合；普通 map + mutex 通常更清晰。
- **性能权衡**：如果为了性能牺牲通用性或可读性，应在代码中用 `// NOTE:` 说明原因，并尽量提供基准测试。

---

#### 8. 测试规范

- **Go 单元测试**：
  - 测试文件放在 `_test.go`。
  - 优先使用表驱动测试，并用 `t.Run()` 命名子测试。
  - 公共 API 优先使用外部测试包（`package foo_test`）；私有逻辑必要时可使用内部包测试。
  - 断言可使用标准库；若项目已引入 `testify`，可使用 `require` / `assert`，不要为少量测试单独新增依赖。
- **测试覆盖重点**：
  - 成功路径、非法参数、空结果、边界值、超时、取消、重试、并发和错误分支。
  - 纯函数优先测试，如参数解析、配置默认值、SQL clause 构建、序列化、响应编码。
  - bug 修复应优先补回归测试。
- **集成测试**：
  - 涉及数据库、网络、中间件、文件系统、系统服务或真实第三方 API 的测试，应明确前置条件。
  - 不稳定或依赖外部环境的测试应使用 build tag、短测跳过或清晰的环境变量开关。
- **并发测试**：涉及共享状态、锁、goroutine 生命周期时优先运行 `go test -race ./...`。
- **基准测试**：性能敏感逻辑需要提供 `BenchmarkXxx`，并说明数据规模假设。
- **测试数据**：使用 `testdata/` 存放测试资源，不使用真实生产数据、密钥或用户隐私。

---

#### 9. 依赖、工具链与构建

- **Go Modules**：必须使用 Go Modules，不使用 GOPATH 模式。
- **依赖管理**：
  - 新增依赖前说明理由、替代方案和维护风险。
  - 不为简单功能引入大型框架。
  - 优先选择维护活跃、API 稳定、许可证兼容、依赖树可控的库。
  - 更新依赖后同步 `go.mod`、`go.sum`，并运行相关验证。
- **构建约束**：
  - 尊重现有 build tag、GOOS/GOARCH、CGO、交叉编译和平台差异。
  - 涉及 Windows、Linux、macOS、容器或特定架构时，不要假设跨平台行为一致。
  - 修改构建脚本、Dockerfile、CI 或发布产物路径时同步文档。
- **常用验证命令**：
  - `gofmt` 或 `goimports`
  - `go test ./...`
  - `go vet ./...`
  - `golangci-lint run`（如果本地已安装或项目配置可用）
  - `go test -race ./...`（涉及并发时优先运行）

---

#### 10. 文档与配置同步

- 修改以下内容时必须同步文档：
  - HTTP/RPC endpoint、请求参数、响应字段、错误码。
  - 配置字段、默认值、环境变量和配置样例。
  - CLI flag、命令示例、退出码和运行前置条件。
  - 数据库 schema、消息协议、事件字段和外部集成契约。
  - 构建方式、部署方式、测试脚本路径和平台要求。
- README 中的命令示例应可直接复制运行；Windows 项目优先提供 PowerShell 或 `.bat` 示例。
- 中文业务文档可以保留中文；代码注释和导出 API 文档优先使用英文。
- 变更公共行为时，应在文档中说明兼容性影响、迁移方式和验证方法。

---

#### 11. AI 执行流程

- **先做上下文调查**：阅读相关代码、测试、配置和文档，再制定实现方式。
- **小步修改**：每次变更聚焦当前需求，避免顺手重构无关代码。
- **完整实现**：生成代码必须可编译，不使用 `...` 省略关键逻辑，不留下无说明的 TODO。
- **安全优先**：处理外部输入时必须考虑边界条件、nil 指针、注入、XSS、路径遍历、资源耗尽和敏感日志。
- **方案评审**：
  - 对业务功能开发或较大重构，编码前至少给出 2 种方案并比较复杂度、可维护性、性能和风险。
  - 明确 bug 修复、单点配置变更、文档更新、小范围测试补充，可直接执行，无需冗长方案评审。
- **编辑纪律**：
  - 不回滚用户未要求回滚的改动。
  - 不做无关格式化或大范围重排。
  - 不引入与需求无关的新依赖、新框架或新目录结构。
- **完成后验证**：能运行测试就运行；无法运行时说明原因和建议的本地验证命令。
- **交付说明**：最终回复应说明改了什么、验证了什么、未验证原因和可能的后续风险。

---

#### 12. 代码评审重点

评审 Go 项目时，优先检查以下问题：

- 外部输入校验是否完整，是否存在注入、路径遍历、XSS、SSRF 或资源耗尽风险。
- 错误是否显式处理，错误链是否保留，客户端错误是否安全。
- `context.Context` 是否贯穿 I/O、数据库、网络和 goroutine 生命周期。
- goroutine、channel、timer、ticker、HTTP body、文件句柄、rows 等资源是否正确关闭。
- 并发访问是否存在 data race、死锁、锁粒度过大或取消不及时。
- 日志是否结构化，是否泄露敏感信息，关键失败是否可观测。
- API、配置、README、示例、测试和默认值是否一致。
- 新增依赖是否必要、维护状态是否可靠、许可证和依赖树是否可接受。
- 平台差异、CGO、build tag、交叉编译和 CI 是否仍然可用。
- 测试是否覆盖成功、失败、边界、并发和回归场景。
